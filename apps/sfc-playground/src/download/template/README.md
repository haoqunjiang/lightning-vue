# Lightning Vue Starter

This is a Vite + Vue starter configured to use `@lightning-vue/compiler` for SFC style compilation.

It requires [Node.js](https://nodejs.org) 22.12+.

To start:

```sh
npm install
npm run dev

# if using yarn:
yarn
yarn dev

# if using pnpm:
pnpm install
pnpm run dev
```
